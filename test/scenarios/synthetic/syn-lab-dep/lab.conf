# Covers lab.dep, which no lab in Kathara-Labs has. lab.dep forces the boot
# order to the exact reverse of the lab.conf declaration order, and each
# device appends its name to a file on the rw-mounted /shared directory.
pc1[0]="A"
pc2[0]="A"
pc3[0]="A"
