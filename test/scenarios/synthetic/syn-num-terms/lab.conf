# Covers num_terms > 0 (the corpus only ever sets num_terms=0).
# Recorded under --noterminals: the golden pins that no terminal is spawned
# and that num_terms leaves no trace in the container.
pc1[0]="A"
pc1[num_terms]=2
