# Covers <device>.shutdown and shared.shutdown, neither of which appears in
# Kathara-Labs. Both run inside the container during lclean; their side effect
# lands on the rw /shared bind mount and is read back from the host after the
# teardown.
pc1[0]="A"
