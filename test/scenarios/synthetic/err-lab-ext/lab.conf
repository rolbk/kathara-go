# Fixture for the lab.ext feature. ExternalLink is DEFERRED post-1.0
# (PORT_SPEC 0.3), so the Go build is expected to reject this scenario while
# Python 3.8.3 accepts it. Recorded in dry mode (--print) so that lab.ext is
# parsed and validated but no host bridge or VLAN interface is ever created.
pc1[0]="A"
