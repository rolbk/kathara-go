# Fixture for the unsupported lab.ext feature. The Go build rejects this
# scenario while Python 3.8.3 accepts it. Recorded in dry mode (--print) so it is
# parsed and validated but no host bridge or VLAN interface is ever created.
pc1[0]="A"
