# Covers the `exec` device option and its append (not overwrite) semantics.
# Both commands must run, in lab.conf order, after the .startup script.
pc1[0]="A"
pc1[exec]="echo first > /exec-marker-a"
pc1[exec]="echo second > /exec-marker-b"
