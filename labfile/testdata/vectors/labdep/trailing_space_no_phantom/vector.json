{
  "description": "Trailing whitespace is removed by line.strip() before matching, so no phantom '' dep is produced.",
  "notes": "strip() prevents a trailing space from creating a phantom empty entry.",
  "parser": "dep"
}
